/**
 * Recebe dois números (a, b)
 * Retorna a soma entre a e b
 * Retorna a + b
 */
function somar(a, b) {
    return a + b;
}

function subtrair(a, b) {
    return a - b;
}

module.exports = {
    somar,
    subtrair
}